<UserControl x:Class="WiseShell.App.Controls.TerminalWebView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid Background="#FF020617">
        <wv2:WebView2 x:Name="Browser" />
        <Border x:Name="FallbackOverlay"
                Background="#FF111827"
                Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="460">
                <TextBlock Margin="0,0,0,10"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="White"
                           Text="WebView2 初始化失败" />
                <TextBlock x:Name="FallbackMessage"
                           Foreground="#FFCBD5E1"
                           Text="当前系统没有可用的 WebView2 运行环境。" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
